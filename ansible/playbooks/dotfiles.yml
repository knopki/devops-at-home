---
- hosts: workstations
  any_errors_fatal: true
  become: true
  become_user: "{{ main_user }}"
  roles:
  - role: dotfiles-environmentd
    tags: environmentd
  - role: dotfiles-sway
    tags: sway
  - role: dotfiles-qt5ct
    tags: qt5ct
