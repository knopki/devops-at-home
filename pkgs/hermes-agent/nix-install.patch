diff --git a/hermes_cli/doctor.py b/hermes_cli/doctor.py
--- a/hermes_cli/doctor.py
+++ b/hermes_cli/doctor.py
@@ -395,7 +395,7 @@ def run_doctor(args):
         check_ok("Node.js")
         # Check if agent-browser is installed
         agent_browser_path = PROJECT_ROOT / "node_modules" / "agent-browser"
-        if agent_browser_path.exists():
+        if shutil.which("agent-browser") or agent_browser_path.exists():
             check_ok("agent-browser (Node.js)", "(browser automation)")
         else:
             check_warn("agent-browser not installed", "(run: npm install)")
diff --git a/hermes_cli/main.py b/hermes_cli/main.py
--- a/hermes_cli/main.py
+++ b/hermes_cli/main.py
@@ -1607,8 +1607,16 @@ def cmd_version(args):
         print("OpenAI SDK: Not installed")
 
 
+def _is_nix_install():
+    return os.getenv("HERMES_NIX_PACKAGE") == "1" or str(PROJECT_ROOT).startswith("/nix/store/")
+
+
 def cmd_uninstall(args):
     """Uninstall Hermes Agent."""
+    if _is_nix_install():
+        print("Hermes Agent is installed via Nix and cannot uninstall itself.")
+        print("Remove it through your flake or profile instead.")
+        return
     from hermes_cli.uninstall import run_uninstall
     run_uninstall(args)
 
@@ -1714,6 +1722,11 @@ def cmd_update(args):
     import subprocess
     import shutil
     
+    if _is_nix_install():
+        print("Hermes Agent is installed via Nix and cannot self-update.")
+        return
+    
     print("⚕ Updating Hermes Agent...")
     print()
     
diff --git a/pyproject.toml b/pyproject.toml
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -26,7 +26,6 @@ dependencies = [
   "prompt_toolkit",
   # Tools
   "firecrawl-py",
-  "fal-client",
   # Text-to-speech (Edge TTS is free, no API key needed)
   "edge-tts",
   # mini-swe-agent deps (terminal tool)
diff --git a/tools/image_generation_tool.py b/tools/image_generation_tool.py
--- a/tools/image_generation_tool.py
+++ b/tools/image_generation_tool.py
@@ -33,7 +33,10 @@ import logging
 import os
 import datetime
 from typing import Dict, Any, Optional, Union
-import fal_client
+try:
+    import fal_client
+except ImportError:
+    fal_client = None
 from tools.debug_helpers import DebugSession
 
 logger = logging.getLogger(__name__)
diff --git a/tools/web_tools.py b/tools/web_tools.py
--- a/tools/web_tools.py
+++ b/tools/web_tools.py
@@ -46,7 +46,10 @@ import os
 import re
 import asyncio
 from typing import List, Dict, Any, Optional
-from firecrawl import Firecrawl
+try:
+    from firecrawl import Firecrawl
+except ImportError:
+    from firecrawl import FirecrawlApp as Firecrawl
 from agent.auxiliary_client import async_call_llm
 from tools.debug_helpers import DebugSession
 
